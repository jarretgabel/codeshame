guard :haml, run_at_start: true, input: '_layouts', output: '_layouts' do
  watch %r{^_layouts/.+\.haml}
end

guard :haml, run_at_start: true, input: '_includes', output: '_includes' do
  watch %r{^_includes/.+\.haml}
end
